problem: sqrt(18*x^2)
outcome: rewritten
result: ((3 * sqrt(2)) * abs(x))
detail: each power law applied with its real-domain condition
context
  application version: nps 0.2
  capability manifest: <current>
  problem family: algebra.powers-and-radicals.one-variable
  envelope version: 1
  normalized problem: sqrt((18 * (x^2)))
  requested method: powsimp
  angle convention: not applicable
  branch convention: real domain, real odd roots, even roots only of values that are not negative
  unit policy: exact integers, decimals as written, no unit conversion
  detail projection: standard
  resource policy: rewrites<=4096 steps<=512 branches<=64 backend<=32
  derivation status: solved and verified
steps
  1 plan
    phase: Plan
    goal: Simplify the powers and roots in sqrt((18 * (x^2)))
    rule: plan.power-laws, Apply the power laws one at a time
    claim: no claim
    explanation: Write roots as powers, apply one law at a time with its condition, then write square roots back.
    detail: Each law is checked for the condition it needs over the real numbers before it is used.
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Apply the power laws one at a time
    applicability: the expression is built from numbers and one variable with powers and roots of single terms
    fact: sqrt((18 * (x^2)))
    rationale: The expression is built from numbers and one variable with powers and roots of single terms.
    verification: one-variable shape reading, passed, Simplify the powers and roots in sqrt((18 * (x^2)))
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    1.1 transformation
      phase: Simplify
      goal: Apply one power law
      rule: pow.root-as-power, Write a square root as a power
      claim: equivalent expression
      explanation: The law used is sqrt(u) = u^(1/2).
      detail: It holds wherever the expression has a real value, so no condition is needed.
      proof obligation: obl.power.same-values, the new form has the value of the old one wherever the old one is real
      action: Write sqrt((18 * (x^2))) as a power of one half.
      before: sqrt((18 * (x^2)))
      after: ((18 * (x^2))^(1 * (2^-1)))
      reversible: yes
      verification: exact evaluation on each branch beyond the degree, passed, both forms agree on every branch of the old form's real domain, at more points than their difference could have roots
      verified: yes
    1.2 transformation
      phase: Simplify
      goal: Apply one power law
      rule: pow.product-power, Raise each factor of a product
      claim: equivalent expression
      explanation: The law used is (uv)^r = u^r v^r, which holds for a root of even index only over factors that are not negative.
      detail: It holds wherever the expression has a real value, so no condition is needed.
      proof obligation: obl.power.same-values, the new form has the value of the old one wherever the old one is real
      action: Raise each factor of (18 * (x^2)).
      before: ((18 * (x^2))^(1 * (2^-1)))
      after: ((18^(1 * (2^-1))) * ((x^2)^(1 * (2^-1))))
      reversible: yes
      verification: exact evaluation on each branch beyond the degree, passed, both forms agree on every branch of the old form's real domain, at more points than their difference could have roots
      verified: yes
    1.3 transformation
      phase: Simplify
      goal: Apply one power law
      rule: pow.numeric-root, Take the root of a number
      claim: equivalent expression
      explanation: The law used is the largest perfect power comes out of a root, and an odd root of a negative number is negative.
      detail: It holds wherever the expression has a real value, so no condition is needed.
      proof obligation: obl.power.same-values, the new form has the value of the old one wherever the old one is real
      action: Take (18^(1 * (2^-1))) as (3 * (2^(1 * (2^-1)))).
      before: ((18^(1 * (2^-1))) * ((x^2)^(1 * (2^-1))))
      after: ((3 * (2^(1 * (2^-1)))) * ((x^2)^(1 * (2^-1))))
      reversible: yes
      verification: exact evaluation on each branch beyond the degree, passed, both forms agree on every branch of the old form's real domain, at more points than their difference could have roots
      verified: yes
    1.4 transformation
      phase: Simplify
      goal: Apply one power law
      rule: pow.power-of-power, Multiply the exponents of a power of a power
      claim: equivalent expression
      explanation: The law used is (u^a)^b = |u|^(ab) when a is even and b is not whole.
      detail: It holds wherever the expression has a real value, so no condition is needed.
      proof obligation: obl.power.same-values, the new form has the value of the old one wherever the old one is real
      action: Multiply the exponents in ((x^2)^(1 * (2^-1))).
      before: ((3 * (2^(1 * (2^-1)))) * ((x^2)^(1 * (2^-1))))
      after: ((3 * (2^(1 * (2^-1)))) * abs(x))
      reversible: yes
      verification: exact evaluation on each branch beyond the degree, passed, both forms agree on every branch of the old form's real domain, at more points than their difference could have roots
      verified: yes
    1.5 transformation
      phase: Simplify
      goal: Apply one power law
      rule: pow.power-as-root, Write a power of one half as a square root
      claim: equivalent expression
      explanation: The law used is u^(1/2) = sqrt(u).
      detail: It holds wherever the expression has a real value, so no condition is needed.
      proof obligation: obl.power.same-values, the new form has the value of the old one wherever the old one is real
      action: Write (2^(1 * (2^-1))) as a square root.
      before: ((3 * (2^(1 * (2^-1)))) * abs(x))
      after: ((3 * sqrt(2)) * abs(x))
      reversible: yes
      verification: exact evaluation on each branch beyond the degree, passed, both forms agree on every branch of the old form's real domain, at more points than their difference could have roots
      verified: yes
    1.6 check
      phase: Check
      goal: Check the result against the expression as typed
      rule: pow.check-values, Check the result on the real domain
      claim: equivalent expression
      explanation: Compare both at enough points of each sign to fix a difference of this degree.
      detail: Substituting a power of t for the variable turns both sides into sums of powers of t, which agree everywhere once they agree at more points than such a sum can vanish.
      proof obligation: obl.power.same-values, the new form has the value of the old one wherever the old one is real
      target claim: ((3 * sqrt(2)) * abs(x)) equals sqrt((18 * (x^2))) wherever it is real
      check method: exact evaluation on each branch beyond the degree
      expected: equal values
      observed: both forms agree on every branch of the old form's real domain, at more points than their difference could have roots
      verification: exact evaluation on each branch beyond the degree, passed, both forms agree on every branch of the old form's real domain, at more points than their difference could have roots
      verified: yes
