problem: flow at 3 m/s through 4 cm^2 narrows to 2 cm^2; find the outlet speed
variable: outlet speed
outcome: solved
result: 6 m/s
context
  application version: nps 0.2
  capability manifest: <current>
  problem family: physics.fluids.continuity.incompressible
  envelope version: <not recorded>
  normalized problem: (v2 = ((A1 * v1) * (A2^-1)))
  requested method: conservation of volume flow rate, exact SI substitution, linear isolation of the requested quantity
  active assumption: the flow is steady and the fluid is incompressible
  active assumption: the speed is uniform across each cross section
  active assumption: no fluid enters or leaves between the two cross sections
  angle convention: <not recorded>
  branch convention: real domain
  unit policy: validate dimensions before substitution, convert exactly to SI, and round only the reported answer
  detail projection: standard
  resource policy: rewrites<=4096 steps<=512 branches<=64 backend<=32
  derivation status: solved and verified
steps
  1 plan
    phase: plan
    goal: Find outlet speed
    rule: physics.fluids.continuity.definition, Continuity of an incompressible flow
    claim: no claim
    explanation: Use v2 = A1*v1*A2^-1 and solve for the requested quantity
    assumption before: the flow is steady and the fluid is incompressible
    assumption before: the speed is uniform across each cross section
    assumption before: no fluid enters or leaves between the two cross sections
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Apply the continuity equation A1*v1 = A2*v2
    applicability: every quantity in v2 = A1*v1*A2^-1 uses compatible dimensions
    applicability: the relation is linear in outlet speed
    fact: inlet area = 4 cm^2
    fact: inlet speed = 3 m/s
    fact: outlet area = 2 cm^2
    fact: find outlet speed
    alternative: general symbolic backend isolation
    rationale: v2 = A1*v1*A2^-1 is linear in the requested unknown after exact SI substitution, so the local exact linear solver is sufficient and deterministic
    verification: dimensional analysis, passed, L T^-1 against L T^-1
    verification: registered relation-position model, passed, v2 occurs at power one in v2 = A1*v1*A2^-1
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    1.1 check
      phase: check
      goal: Check the dimensions of v2 = A1*v1*A2^-1
      rule: physics.fluids.continuity.check-dimensions, Dimensional analysis
      claim: definition
      explanation: Both sides of v2 = A1*v1*A2^-1 must have the same dimension
      proof obligation: physics.fluids.continuity.dimensions-agree, both sides of v2 = A1*v1*A2^-1 have the same dimension
      target claim: v2 = A1*v1*A2^-1 is dimensionally consistent
      check method: raise each factor dimension to its power and multiply
      expected: equal dimensions
      observed: L T^-1 against L T^-1
      verification: dimensional analysis, passed, L T^-1 against L T^-1
      verified: yes
    1.2 transformation
      phase: solve
      goal: Convert the known quantities to SI
      rule: physics.fluids.continuity.convert-units, Unit conversion
      claim: solution set preserved
      explanation: Apply each unit's exact scale to SI
      detail: Convert every known quantity with its exact unit scale before substituting into v2 = A1*v1*A2^-1, so the relation uses consistent SI units throughout.
      proof obligation: obl.physics.scale-preserves-solutions, each quantity's stored SI value is its given value times the table factor
      action: inlet area = 4 cm^2 becomes inlet area = 0.0004 m^2, outlet area = 2 cm^2 becomes outlet area = 0.0002 m^2
      before: (v2 = ((A1 * v1) * (A2^-1)))
      after: (v2 = ((A1 * v1) * (A2^-1)))
      reversible: yes
      verification: divide each stored value by its table scale and compare with the given, passed, 3 stored values divide back to the given by their table scale
      verified: yes
    1.3 transformation
      phase: solve
      goal: Substitute the known SI values
      rule: physics.fluids.continuity.substitute, Substitution
      claim: solution set preserved
      explanation: Replace each known symbol by its exact SI value
      detail: After converting the known quantities to SI, put their values into v2 = A1*v1/A2. Leave the requested quantity as a symbol, then solve the resulting equation for it.
      proof obligation: obl.physics.lookup-preserves-solutions, the value put in place of a symbol is the one the problem declared for it
      action: Substitute the known quantities into v2 = A1*v1*A2^-1 after exact SI conversion
      before: (v2 = ((A1 * v1) * (A2^-1)))
      after: (v2 = (((1 * (2500^-1)) * 3) * ((1 * (5000^-1))^-1)))
      reversible: yes
      verification: typed known-quantity lookup, passed, 3 known quantities were substituted
      verified: yes
    1.4 check
      phase: check
      goal: Check the candidate in v2 = A1*v1*A2^-1
      rule: physics.fluids.continuity.check-candidate, Substitution check
      claim: implication
      explanation: Put the candidate back into v2 = A1*v1*A2^-1
      proof obligation: physics.fluids.continuity.candidate-satisfies, the candidate satisfies v2 = A1*v1*A2^-1
      target claim: v2 = 6 satisfies v2 = A1*v1*A2^-1
      check method: substitute every exact SI value into the relation
      expected: both sides of v2 = A1*v1*A2^-1 agree
      observed: both sides are exactly equal
      verification: exact substitution into the original relation, passed, both sides are exactly equal
      verified: yes
  2 plan
    phase: plan
    goal: Isolate v2
    rule: eq.linear.inverse-operations, Inverse operations on a linear equation
    claim: no claim
    explanation: Solve by undoing what was done to v2
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Inverse operations on a linear equation
    applicability: the equation is degree one in v2
    applicability: every other quantity is a rational constant
    fact: one unknown, v2
    rationale: collect the terms, remove the constant and isolate v2 using its non-zero coefficient, or inspect the remaining constant if the coefficient is zero
    verification: exact linear analysis, passed, both sides reduced to a coefficient and constant term
    verification: exact linear analysis, passed, all coefficients and constants are exact rationals
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    2.1 transformation
      phase: solve
      goal: Collect the terms in v2
      rule: eq.collect-like-terms, Collect like terms
      claim: solution set preserved
      explanation: Moving every term to one side keeps both sides equal
      detail: Reach for this whenever the unknown appears more than once, on either side or both. Adding or subtracting the same quantity from both sides leaves the equation with the same solutions, so the terms in the unknown can all be gathered on one side and the plain numbers on the other without changing the answer.
      proof obligation: obl.eq.same-solutions, the rewritten equation has the solutions the original had
      action: Move every term to the left side
      before: (v2 = (((1 * (2500^-1)) * 3) * ((1 * (5000^-1))^-1)))
      after: (((1 * v2) + -6) = 0)
      reversible: yes
      verification: rule-local equality invariant, passed, the same quantity was added to both sides, which cannot change the solution set
      verified: yes
    2.2 transformation
      phase: solve
      goal: Isolate v2
      rule: eq.divide-both-sides, Isolate the unknown
      claim: solution set preserved
      explanation: Applying the same operation to both sides preserves the solutions
      detail: Remove the constant term by applying its opposite to both sides. The coefficient of v2 is 1, so no division is needed.
      proof obligation: obl.eq.same-solutions, the rewritten equation has the solutions the original had
      action: Add 6 to both sides
      before: (((1 * v2) + -6) = 0)
      after: (v2 = 6)
      reversible: yes
      verification: rule-local equality invariant, passed, removing the constant preserves equality, and the coefficient 1 was checked to be non-zero
      verified: yes
    2.3 check
      phase: check
      goal: Check the answer
      rule: eq.linear.check-by-substitution, Check by substitution
      claim: solution set preserved
      explanation: Put the answer back into the original equation
      detail: Put the candidate back where the unknown was in the equation as it was typed and work both sides out. A solution makes them equal, so anything else means the candidate is not one and the answer is withheld rather than reported.
      proof obligation: obl.linear.candidate-satisfies, the candidate satisfies the original equation
      target claim: v2 = 6 satisfies the equation
      check method: substitute the solution into the equation as it was typed
      expected: both sides equal
      observed: both sides are equal
      verification: substitution, passed, both sides came out equal
      verified: yes
