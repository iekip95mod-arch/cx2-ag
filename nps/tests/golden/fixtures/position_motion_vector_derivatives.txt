problem: r(t) = 3t i + 2t^2 j over 0 to 2 s, read at 2 s
outcome: solved
result: average velocity (3, 4) m/s; instantaneous velocity (3, 8) m/s; instantaneous acceleration (0, 4) m/s^2
context
  application version: nps 0.2
  capability manifest: <current>
  problem family: physics.motion.position-vector
  envelope version: 1
  requested method: differentiate each component of the supplied position vector twice in t, take the secant over the declared interval for the average velocity and evaluate both derivatives at the event time
  active assumption: each component of the position vector is a function of t alone
  active assumption: the axes are independent and share one clock
  angle convention: <not recorded>
  branch convention: <not recorded>
  unit policy: interval bounds and the event time are converted to seconds exactly, and each reported vector carries the SI unit of its own dimension
  detail projection: standard
  resource policy: rewrites<=4096 steps<=512 branches<=64 backend<=32
  derivation status: solved and verified
steps
  1 plan
    phase: plan
    goal: Differentiate (3 * t) with respect to t
    rule: calculus.differentiate.rules, Differentiate by rule
    claim: no claim
    explanation: Work outwards in, applying the rule that matches each form
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Differentiate by rule
    applicability: every form in the expression has a derivative rule
    fact: differentiate with respect to t
    rationale: each rule is applied to the form it matches and the parts are differentiated in turn, so every line of the result can be traced to the rule that produced it
    verification: registered derivative rule dispatch, passed, every visited form matched a registered derivative rule
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    1.1 transformation
      phase: differentiate
      goal: Differentiate (3 * t)
      rule: d.constant-multiple, Constant multiple rule
      claim: equivalent expression
      explanation: A constant factor stays where it is and the rest is differentiated
      detail: Reach for this when one factor has no variable in it, as in 3 sin(x) or x squared over 2. Scaling something by a fixed number scales its rate of change by the same number, so the constant can be lifted out of the way and put back afterwards. This is not the product rule: that one is for when both factors change.
      proof obligation: obl.calculus.rule-preserves-value, the rewritten subexpression has the value the original had
      action: Take 3 outside the derivative
      before: d((3 * t), t)
      after: (3 * 1)
      reversible: no
      verification: rule-local invariant, passed, the factor taken out contains no occurrence of the variable
      verified: yes
      1.1.1 transformation
        phase: differentiate
        goal: Differentiate t
        rule: d.variable, Derivative of the variable
        claim: equivalent expression
        explanation: The variable changes at the same rate as itself
        detail: Reach for this when the expression is the bare variable and nothing else. If x grows by some amount then x grows by exactly that amount, so the rate of change is one. It is the power rule with an exponent of one, written out separately because it comes up constantly as the last step of a longer derivative.
        proof obligation: obl.calculus.rule-preserves-value, the rewritten subexpression has the value the original had
        action: Replace the derivative of the variable with one
        before: d(t, t)
        after: 1
        reversible: no
        verification: rule-local invariant, passed, the expression is the variable itself
        verified: yes
  2 plan
    phase: plan
    goal: Differentiate (3 * 1) with respect to t
    rule: calculus.differentiate.rules, Differentiate by rule
    claim: no claim
    explanation: Work outwards in, applying the rule that matches each form
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Differentiate by rule
    applicability: every form in the expression has a derivative rule
    fact: differentiate with respect to t
    rationale: each rule is applied to the form it matches and the parts are differentiated in turn, so every line of the result can be traced to the rule that produced it
    verification: registered derivative rule dispatch, passed, every visited form matched a registered derivative rule
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    2.1 transformation
      phase: differentiate
      goal: Differentiate (3 * 1)
      rule: d.constant, Constant rule
      claim: equivalent expression
      explanation: A quantity that does not change has derivative zero
      detail: Reach for this whenever the expression has no occurrence of the variable in it, however complicated it looks: a plain number, pi, or a second letter being held fixed all qualify. A derivative measures how fast something changes as the variable moves, and none of these move at all, so the answer is zero.
      proof obligation: obl.calculus.rule-preserves-value, the rewritten subexpression has the value the original had
      action: Replace the derivative of a constant with zero
      before: d((3 * 1), t)
      after: 0
      reversible: no
      verification: rule-local invariant, passed, the expression contains no occurrence of the variable
      verified: yes
  3 plan
    phase: plan
    goal: Differentiate (2 * (t^2)) with respect to t
    rule: calculus.differentiate.rules, Differentiate by rule
    claim: no claim
    explanation: Work outwards in, applying the rule that matches each form
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Differentiate by rule
    applicability: every form in the expression has a derivative rule
    fact: differentiate with respect to t
    rationale: each rule is applied to the form it matches and the parts are differentiated in turn, so every line of the result can be traced to the rule that produced it
    verification: registered derivative rule dispatch, passed, every visited form matched a registered derivative rule
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    3.1 transformation
      phase: differentiate
      goal: Differentiate (2 * (t^2))
      rule: d.constant-multiple, Constant multiple rule
      claim: equivalent expression
      explanation: A constant factor stays where it is and the rest is differentiated
      detail: Reach for this when one factor has no variable in it, as in 3 sin(x) or x squared over 2. Scaling something by a fixed number scales its rate of change by the same number, so the constant can be lifted out of the way and put back afterwards. This is not the product rule: that one is for when both factors change.
      proof obligation: obl.calculus.rule-preserves-value, the rewritten subexpression has the value the original had
      action: Take 2 outside the derivative
      before: d((2 * (t^2)), t)
      after: (2 * (2 * (t^1)))
      reversible: no
      verification: rule-local invariant, passed, the factor taken out contains no occurrence of the variable
      verified: yes
      3.1.1 transformation
        phase: differentiate
        goal: Differentiate (t^2)
        rule: d.power, Power rule
        claim: equivalent expression
        explanation: Bring the exponent down and reduce it by one
        detail: Reach for this when the variable itself is raised to a fixed whole number, as in x squared or x cubed. The old exponent comes down to the front as a multiplier and the new one is smaller by one, so x to the n becomes n times x to the n minus one. A negative exponent works the same way, which is why one over x squared differentiates without a separate rule.
        proof obligation: obl.calculus.rule-preserves-value, the rewritten subexpression has the value the original had
        action: Apply the power rule with exponent 2
        before: d((t^2), t)
        after: (2 * (t^1))
        reversible: no
        verification: rule-local invariant, passed, the base is the variable and the exponent is a constant integer
        verified: yes
  4 plan
    phase: plan
    goal: Differentiate (2 * (2 * (t^1))) with respect to t
    rule: calculus.differentiate.rules, Differentiate by rule
    claim: no claim
    explanation: Work outwards in, applying the rule that matches each form
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Differentiate by rule
    applicability: every form in the expression has a derivative rule
    fact: differentiate with respect to t
    rationale: each rule is applied to the form it matches and the parts are differentiated in turn, so every line of the result can be traced to the rule that produced it
    verification: registered derivative rule dispatch, passed, every visited form matched a registered derivative rule
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    4.1 transformation
      phase: differentiate
      goal: Differentiate (2 * (2 * (t^1)))
      rule: d.constant-multiple, Constant multiple rule
      claim: equivalent expression
      explanation: A constant factor stays where it is and the rest is differentiated
      detail: Reach for this when one factor has no variable in it, as in 3 sin(x) or x squared over 2. Scaling something by a fixed number scales its rate of change by the same number, so the constant can be lifted out of the way and put back afterwards. This is not the product rule: that one is for when both factors change.
      proof obligation: obl.calculus.rule-preserves-value, the rewritten subexpression has the value the original had
      action: Take 2 outside the derivative
      before: d((2 * (2 * (t^1))), t)
      after: (2 * (2 * (1 * (t^0))))
      reversible: no
      verification: rule-local invariant, passed, the factor taken out contains no occurrence of the variable
      verified: yes
      4.1.1 transformation
        phase: differentiate
        goal: Differentiate (2 * (t^1))
        rule: d.constant-multiple, Constant multiple rule
        claim: equivalent expression
        explanation: A constant factor stays where it is and the rest is differentiated
        detail: Reach for this when one factor has no variable in it, as in 3 sin(x) or x squared over 2. Scaling something by a fixed number scales its rate of change by the same number, so the constant can be lifted out of the way and put back afterwards. This is not the product rule: that one is for when both factors change.
        proof obligation: obl.calculus.rule-preserves-value, the rewritten subexpression has the value the original had
        action: Take 2 outside the derivative
        before: d((2 * (t^1)), t)
        after: (2 * (1 * (t^0)))
        reversible: no
        verification: rule-local invariant, passed, the factor taken out contains no occurrence of the variable
        verified: yes
        4.1.1.1 transformation
          phase: differentiate
          goal: Differentiate (t^1)
          rule: d.power, Power rule
          claim: equivalent expression
          explanation: Bring the exponent down and reduce it by one
          detail: Reach for this when the variable itself is raised to a fixed whole number, as in x squared or x cubed. The old exponent comes down to the front as a multiplier and the new one is smaller by one, so x to the n becomes n times x to the n minus one. A negative exponent works the same way, which is why one over x squared differentiates without a separate rule.
          proof obligation: obl.calculus.rule-preserves-value, the rewritten subexpression has the value the original had
          action: Apply the power rule with exponent 1
          before: d((t^1), t)
          after: (1 * (t^0))
          reversible: no
          verification: rule-local invariant, passed, the base is the variable and the exponent is a constant integer
          verified: yes
