problem: x + 1 = x + 2
variable: x
outcome: no solution
detail: the unknown cancels and leaves a false statement, so nothing satisfies it
context
  application version: nps 0.2
  capability manifest: <current>
  problem family: algebra.linear-equation.one-unknown
  envelope version: <not recorded>
  normalized problem: ((x + 1) = (x + 2))
  requested method: inverse operations
  angle convention: radians
  branch convention: real domain
  unit policy: exact integers, decimals as written, no unit conversion
  detail projection: standard
  resource policy: rewrites<=4096 steps<=512 branches<=64 backend<=32
  derivation status: solved and verified
steps
  1 plan
    phase: plan
    goal: Isolate x
    rule: eq.linear.inverse-operations, Inverse operations on a linear equation
    claim: no claim
    explanation: Solve by undoing what was done to x
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Inverse operations on a linear equation
    applicability: the equation is degree one in x
    applicability: every other quantity is a rational constant
    fact: one unknown, x
    rationale: collect the terms, remove the constant and isolate x using its non-zero coefficient, or inspect the remaining constant if the coefficient is zero
    verification: exact linear analysis, passed, both sides reduced to a coefficient and constant term
    verification: exact linear analysis, passed, all coefficients and constants are exact rationals
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    1.1 transformation
      phase: solve
      goal: Collect the terms in x
      rule: eq.collect-like-terms, Collect like terms
      claim: solution set preserved
      explanation: Moving every term to one side keeps both sides equal
      detail: Reach for this whenever the unknown appears more than once, on either side or both. Adding or subtracting the same quantity from both sides leaves the equation with the same solutions, so the terms in the unknown can all be gathered on one side and the plain numbers on the other without changing the answer.
      proof obligation: obl.eq.same-solutions, the rewritten equation has the solutions the original had
      action: Move every term to the left side
      before: ((x + 1) = (x + 2))
      after: (((0 * x) + -1) = 0)
      reversible: yes
      verification: rule-local equality invariant, passed, the same quantity was added to both sides, which cannot change the solution set
      verified: yes
    1.2 check
      phase: solve
      goal: Read off the outcome
      rule: eq.linear.inspect-collected-coefficient, Coefficient inspection
      claim: solution set preserved
      explanation: The unknown cancels and what is left is false
      detail: Once every term in the unknown is collected on one side, the coefficient of the unknown decides the shape of the answer: if it is zero the unknown has cancelled, and then the constant term says whether what is left is true for every value or for none.
      proof obligation: obl.linear.coefficient-decides, the collected coefficient of the unknown is what decides between no solution and every value
      target claim: the equation has no term in x
      check method: inspect the collected coefficient
      expected: coefficient of x is zero
      observed: constant term is not zero
      verification: inspection of the collected coefficient, passed, the coefficient of the unknown is zero, which is what decides between no solution and every value
      verified: yes
