problem: x/(x^2-4) - 1/(x-2)
variable: x
outcome: rewritten
result: (-2 * (((x^2) + -4)^-1))
detail: one reduced fraction with every excluded value kept
context
  application version: nps 0.2
  capability manifest: <current>
  problem family: algebra.rational-expression.single-variable
  envelope version: 1
  normalized problem: ((x * (((x^2) + (-4))^(-1))) + (-(1 * ((x + (-2))^(-1)))))
  requested method: normal
  active assumption: (x + -2) is not zero
  active assumption: (x + 2) is not zero
  angle convention: not applicable
  branch convention: real domain, excluded values kept
  unit policy: exact integers, decimals as written, no unit conversion
  detail projection: standard
  resource policy: rewrites<=4096 steps<=512 branches<=64 backend<=32
  derivation status: solved and verified
steps
  1 plan
    phase: Plan
    goal: Write ((x * (((x^2) + (-4))^(-1))) + (-(1 * ((x + (-2))^(-1))))) as one reduced fraction
    rule: plan.rational-normal, Combine into one fraction and cancel common factors
    claim: no claim
    explanation: Note the excluded values, write one fraction, then cancel what the top and bottom share.
    detail: A value that makes any denominator zero is excluded from the start, and it stays excluded even when the factor that caused it cancels.
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Combine into one fraction and cancel common factors
    applicability: every part is a polynomial in the variable with exact rational coefficients and degree at most 12, or a quotient of such
    fact: ((x * (((x^2) + (-4))^(-1))) + (-(1 * ((x + (-2))^(-1)))))
    rationale: Every part is a polynomial in x with exact rational coefficients, or a quotient of such.
    verification: exact rational function reading, passed, Write ((x * (((x^2) + (-4))^(-1))) + (-(1 * ((x + (-2))^(-1))))) as one reduced fraction
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    1.1 check
      phase: Domain
      goal: Find the values the expression excludes
      rule: rat.excluded-values, Exclude the zeros of every denominator
      claim: definition
      explanation: The expression has no value where a denominator is zero: (x + -2) is not zero, (x + 2) is not zero.
      detail: These conditions hold for every later form, including one where the factor has cancelled.
      domain restriction: (x + -2) is not zero
      domain restriction: (x + 2) is not zero
      proof obligation: obl.rational.excluded-values, every excluded value makes one of the denominators zero
      target claim: The expression is defined exactly when (x + -2) is not zero, (x + 2) is not zero
      check method: exact denominator evaluation
      expected: each excluded value is a zero of a denominator
      observed: (x + -2) is not zero, (x + 2) is not zero
      verification: exact denominator evaluation, passed, each excluded value makes its denominator exactly zero
      verified: yes
    1.2 transformation
      phase: Combine
      goal: Write the expression as one fraction
      rule: rat.common-denominator, Write over a common denominator
      claim: equivalent expression
      explanation: Write every term over the same denominator, then add the numerators.
      detail: A sum needs every term over the same denominator before the numerators add. A product multiplies the numerators together and the denominators together.
      proof obligation: obl.rational.same-values, the new form has the value of the old one wherever both are defined
      action: Write (((-2 * x) + 4) * (((x^3) + (-2 * (x^2)) + (-4 * x) + 8)^-1)).
      before: ((x * (((x^2) + (-4))^(-1))) + (-(1 * ((x + (-2))^(-1)))))
      after: (((-2 * x) + 4) * (((x^3) + (-2 * (x^2)) + (-4 * x) + 8)^-1))
      reversible: yes
      verification: exact evaluation at more points than the cross-multiplied degree, passed, both sides agree at 25 points, more than the degree 24 their cross-multiplied difference can have, read from the polynomials
      verified: yes
    1.3 transformation
      phase: Cancel
      goal: Cancel the factor the numerator and denominator share
      rule: rat.cancel-common-factor, Cancel a common factor
      claim: equivalent expression
      explanation: Divide the top and the bottom by (x + -2).
      detail: Where (x + -2) is zero the original expression has no value, so those values stay excluded even though the factor is gone from the result.
      proof obligation: obl.rational.exact-cancellation, the numerator and the denominator are each the reduced one times the cancelled factor
      action: Cancel (x + -2) to get (-2 * (((x^2) + -4)^-1)).
      before: (((-2 * x) + 4) * (((x^3) + (-2 * (x^2)) + (-4 * x) + 8)^-1))
      after: (-2 * (((x^2) + -4)^-1))
      reversible: no
      verification: exact polynomial division, passed, multiplying back by the cancelled factor returns the numerator and the denominator exactly
      verified: yes
    1.4 check
      phase: Check
      goal: Check the result against the expression as typed
      rule: rat.check-equivalent, Check the result has the same values
      claim: equivalent expression
      explanation: Evaluate both at enough points to fix a rational function of this degree.
      detail: Two fractions of polynomials that agree at more points than the degree of their cross-multiplied difference are the same function wherever both are defined.
      proof obligation: obl.rational.same-values, the new form has the value of the old one wherever both are defined
      target claim: (-2 * (((x^2) + -4)^-1)) equals ((x * (((x^2) + (-4))^(-1))) + (-(1 * ((x + (-2))^(-1))))) wherever both are defined
      check method: exact evaluation at more points than the cross-multiplied degree
      expected: equal values
      observed: both sides agree at 6 points, more than the degree 5 their cross-multiplied difference can have, read from the polynomials
      verification: exact evaluation at more points than the cross-multiplied degree, passed, both sides agree at 6 points, more than the degree 5 their cross-multiplied difference can have, read from the polynomials
      verified: yes
