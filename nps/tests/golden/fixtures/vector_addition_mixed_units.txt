problem: (0.00120, 0.0020) km + (5, 5) m
variable: sum
outcome: solved
result: (6.2 i + 7.0 j) m
context
  application version: nps 0.2
  capability manifest: <current>
  problem family: physics.vectors.cartesian-addition.two-dimension
  envelope version: <not recorded>
  normalized problem: vector_add(framed_vector(vector((3 * (2500^-1)), (1 * (500^-1))), 1000, lab, dimension(1, 0, 0)), framed_vector(vector(5, 5), 1, lab, dimension(1, 0, 0)))
  requested method: convert components to SI, add matching components, report once
  active assumption: first vector frame is lab
  active assumption: second vector frame is lab
  angle convention: <not recorded>
  branch convention: <not recorded>
  unit policy: exact SI conversion and component arithmetic, final-only precision
  detail projection: standard
  resource policy: rewrites<=4096 steps<=512 branches<=64 backend<=32
  derivation status: solved and verified
steps
  1 plan
    phase: plan
    goal: Add the two Cartesian vectors
    rule: vec.add.plan, Cartesian vector addition
    claim: no claim
    explanation: Check compatibility, convert to SI, then add i and j components
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Cartesian vector addition
    applicability: both vectors have two components
    applicability: both vectors use the same frame
    applicability: both vectors have the same physical dimension
    fact: first vector: (0.0012 i + 0.002 j) km
    fact: second vector: (5 i + 5 j) m
    rationale: matching Cartesian components add after exact SI conversion
    verification: rank comparison, passed, ranks 2 and 2
    verification: frame identity, passed, frame lab and frame lab
    verification: dimension comparison, passed, L and L
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    1.1 check
      phase: check
      goal: Check vector ranks
      rule: vec.add.check-rank, Vector addition precondition
      claim: definition
      explanation: This archetype adds two-dimensional vectors
      proof obligation: obl.vector-add.rank-two, both input vectors have rank two
      target claim: both vectors are two-dimensional
      check method: rank comparison
      expected: rank 2 and rank 2
      observed: ranks 2 and 2
      verification: rank comparison, passed, ranks 2 and 2
      verified: yes
    1.2 check
      phase: check
      goal: Check vector frames
      rule: vec.add.check-frame, Vector addition precondition
      claim: definition
      explanation: Frames must match unless a basis transformation is explicit
      proof obligation: obl.vector-add.frames-match, both input vectors use the same frame
      target claim: both vectors use the same frame
      check method: frame identity
      expected: identical named frames
      observed: frame lab and frame lab
      verification: frame identity, passed, frame lab and frame lab
      verified: yes
    1.3 check
      phase: check
      goal: Check vector dimensions
      rule: vec.add.check-dimension, Vector addition precondition
      claim: definition
      explanation: Only quantities of the same dimension can be added
      proof obligation: obl.vector-add.dimensions-match, both input vectors have the same physical dimension
      target claim: both vector units have the same dimension
      check method: dimension comparison
      expected: equal dimensions
      observed: L and L
      verification: dimension comparison, passed, L and L
      verified: yes
    1.4 transformation
      phase: solve
      goal: Convert both vectors to SI
      rule: vec.add.convert-si, Exact SI conversion
      claim: equivalent expression
      explanation: Apply each unit's exact scale to every component
      detail: Reach for this whenever the two vectors are written in different units, or in any unit that is not the SI one. Adding a component in kilometres to one in metres would be adding two different things, so every component is put on the same scale first. The scale factors are exact, so nothing is lost here and nothing needs rounding yet.
      proof obligation: obl.physics.converts-by-table, the SI form comes from the unit table's exact factor for the entered unit
      action: (0.0012 i + 0.002 j) km becomes (1.2 i + 2 j) m, and (5 i + 5 j) m becomes (5 i + 5 j) m
      before: vector_add(framed_vector(vector((3 * (2500^-1)), (1 * (500^-1))), 1000, lab, dimension(1, 0, 0)), framed_vector(vector(5, 5), 1, lab, dimension(1, 0, 0)))
      after: vector_add(framed_vector(vector((6 * (5^-1)), 2), 1, lab, dimension(1, 0, 0)), framed_vector(vector(5, 5), 1, lab, dimension(1, 0, 0)))
      reversible: yes
      verification: unit table, passed, both unit scales were applied exactly
      verified: yes
    1.5 transformation
      phase: solve
      goal: Add the i components
      rule: vec.add.component-i, Component addition
      claim: equivalent expression
      explanation: Components on the same Cartesian axis add as exact scalars
      detail: Reach for this once both vectors are in the same units and you want their sum. Vectors add one axis at a time: the two i components make the answer's i component, and the two j components make its j. Each of those is ordinary addition of numbers, which is the whole reason breaking a vector into components is worth doing.
      proof obligation: obl.vector-add.component-sum, the component written down is the exact sum of the two input components
      action: 1.2 + 5 = 6.2
      before: ((6 * (5^-1)) + 5)
      after: (31 * (5^-1))
      reversible: yes
      verification: exact rational addition, passed, 6.2
      verified: yes
    1.6 transformation
      phase: solve
      goal: Add the j components
      rule: vec.add.component-j, Component addition
      claim: equivalent expression
      explanation: Components on the same Cartesian axis add as exact scalars
      detail: Reach for this once both vectors are in the same units and you want their sum. Vectors add one axis at a time: the two i components make the answer's i component, and the two j components make its j. Each of those is ordinary addition of numbers, which is the whole reason breaking a vector into components is worth doing.
      proof obligation: obl.vector-add.component-sum, the component written down is the exact sum of the two input components
      action: 2 + 5 = 7
      before: (2 + 5)
      after: 7
      reversible: yes
      verification: exact rational addition, passed, 7
      verified: yes
    1.7 transformation
      phase: report
      goal: Apply final reporting precision
      rule: vec.add.report-precision, Final-only precision
      claim: definition
      explanation: Round only the reported vector, after exact component addition
      detail: Reach for this once, at the very end, and never partway through. A measured value is only as good as the figures it was written with, so the answer is reported to the fewest significant figures among the measurements it came from. Rounding a component before adding would throw away figures the final rounding cannot get back, which is why every step above this one keeps the exact value.
      proof obligation: obl.vector-add.rounding-final, precision is applied once after exact component addition
      action: report (6.2 i + 7.0 j) m
      before: vector((31 * (5^-1)), 7)
      after: vector((31 * (5^-1)), 7)
      reversible: no
      verification: significant figures, passed, 2
      verified: yes
