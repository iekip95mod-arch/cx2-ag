problem: (x^3+x)/(x^2-1)
variable: x
outcome: rewritten
result: (x + (1 * ((x + -1)^-1)) + (1 * ((x + 1)^-1)))
detail: partial fractions over the linear factors with every excluded value kept
context
  application version: nps 0.2
  capability manifest: <current>
  problem family: algebra.partial-fractions.linear-factors
  envelope version: 1
  normalized problem: (((x^3) + x) * (((x^2) + (-1))^(-1)))
  requested method: partial fractions
  active assumption: (x + -1) is not zero
  active assumption: (x + 1) is not zero
  angle convention: not applicable
  branch convention: real domain, excluded values kept
  unit policy: exact integers, decimals as written, no unit conversion
  detail projection: standard
  resource policy: rewrites<=4096 steps<=512 branches<=64 backend<=32
  derivation status: solved and verified
steps
  1 plan
    phase: Plan
    goal: Write (((x^3) + x) * (((x^2) + (-1))^(-1))) as partial fractions
    rule: plan.rational-partial-fractions, Reduce, divide, then split over the linear factors
    claim: no claim
    explanation: Reduce to one fraction, divide out any polynomial part, then give each linear factor its own fraction.
    detail: A value that makes any denominator zero is excluded from the start, and it stays excluded even when the factor that caused it cancels.
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Reduce, divide, then split over the linear factors
    applicability: the reduced denominator is a product of distinct linear factors with rational roots
    applicability: every part is a polynomial in the variable with exact rational coefficients and degree at most 12, or a quotient of such
    fact: (((x^3) + x) * (((x^2) + (-1))^(-1)))
    rationale: Every part is a polynomial in x with exact rational coefficients, or a quotient of such.
    verification: rational root search, passed, 2 distinct rational roots and nothing left over
    verification: exact rational function reading, passed, Write (((x^3) + x) * (((x^2) + (-1))^(-1))) as partial fractions
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    1.1 check
      phase: Domain
      goal: Find the values the expression excludes
      rule: rat.excluded-values, Exclude the zeros of every denominator
      claim: definition
      explanation: The expression has no value where a denominator is zero: (x + -1) is not zero, (x + 1) is not zero.
      detail: These conditions hold for every later form, including one where the factor has cancelled.
      domain restriction: (x + -1) is not zero
      domain restriction: (x + 1) is not zero
      proof obligation: obl.rational.excluded-values, every excluded value makes one of the denominators zero
      target claim: The expression is defined exactly when (x + -1) is not zero, (x + 1) is not zero
      check method: exact denominator evaluation
      expected: each excluded value is a zero of a denominator
      observed: (x + -1) is not zero, (x + 1) is not zero
      verification: exact denominator evaluation, passed, each excluded value makes its denominator exactly zero
      verified: yes
    1.2 transformation
      phase: Combine
      goal: Write the expression as one fraction
      rule: rat.single-fraction, Expand the numerator and the denominator
      claim: equivalent expression
      explanation: Expand the numerator and the denominator.
      detail: A sum needs every term over the same denominator before the numerators add. A product multiplies the numerators together and the denominators together.
      proof obligation: obl.rational.same-values, the new form has the value of the old one wherever both are defined
      action: Write (((x^3) + x) * (((x^2) + -1)^-1)).
      before: (((x^3) + x) * (((x^2) + (-1))^(-1)))
      after: (((x^3) + x) * (((x^2) + -1)^-1))
      reversible: yes
      verification: exact evaluation at more points than the cross-multiplied degree, passed, both sides agree at 25 points, more than the degree 24 their cross-multiplied difference can have, read from the polynomials
      verified: yes
    1.3 transformation
      phase: Divide
      goal: Divide out the polynomial part
      rule: pf.divide, Divide the numerator by the denominator
      claim: equivalent expression
      explanation: The numerator's degree is not below the denominator's, so divide first.
      detail: The quotient is the polynomial part and the remainder over the same denominator is what gets split.
      proof obligation: obl.rational.exact-division, the quotient times the denominator plus the remainder is the numerator
      action: Divide to get (x + ((2 * x) * (((x^2) + -1)^-1))).
      before: (((x^3) + x) * (((x^2) + -1)^-1))
      after: (x + ((2 * x) * (((x^2) + -1)^-1)))
      reversible: yes
      verification: exact polynomial division, passed, the quotient times the denominator plus the remainder gives the numerator exactly
      verified: yes
    1.4 check
      phase: Split
      goal: Find the numerator over (x + -1)
      rule: pf.cover-up, Cover up the factor and evaluate at its root
      claim: definition
      explanation: Cover up (x + -1) and put x = 1 into what is left.
      detail: Every other term vanishes at this root, so the numerator of this term is the remainder divided by the other factors there.
      proof obligation: obl.rational.cover-up, the coefficient times the other factors at the root equals the remainder at the root
      target claim: The numerator over (x + -1) is 1
      check method: exact evaluation at the root
      expected: the coefficient times the other factors equals the remainder at the root
      observed: 1
      verification: exact evaluation at the root, passed, the coefficient times the other factors at the root gives the remainder there exactly
      verified: yes
    1.5 check
      phase: Split
      goal: Find the numerator over (x + 1)
      rule: pf.cover-up, Cover up the factor and evaluate at its root
      claim: definition
      explanation: Cover up (x + 1) and put x = -1 into what is left.
      detail: Every other term vanishes at this root, so the numerator of this term is the remainder divided by the other factors there.
      proof obligation: obl.rational.cover-up, the coefficient times the other factors at the root equals the remainder at the root
      target claim: The numerator over (x + 1) is 1
      check method: exact evaluation at the root
      expected: the coefficient times the other factors equals the remainder at the root
      observed: 1
      verification: exact evaluation at the root, passed, the coefficient times the other factors at the root gives the remainder there exactly
      verified: yes
    1.6 transformation
      phase: Split
      goal: Write the sum of partial fractions
      rule: pf.decompose, Write one fraction for each linear factor
      claim: equivalent expression
      explanation: Add the fractions found for each factor.
      detail: The denominators are the linear factors and the numerators are the numbers the cover-up gave.
      proof obligation: obl.rational.same-values, the new form has the value of the old one wherever both are defined
      action: Write (x + (1 * ((x + -1)^-1)) + (1 * ((x + 1)^-1))).
      before: (x + ((2 * x) * (((x^2) + -1)^-1)))
      after: (x + (1 * ((x + -1)^-1)) + (1 * ((x + 1)^-1)))
      reversible: yes
      verification: exact evaluation at more points than the cross-multiplied degree, passed, both sides agree at 25 points, more than the degree 24 their cross-multiplied difference can have, read from the polynomials
      verified: yes
    1.7 check
      phase: Check
      goal: Check the result against the expression as typed
      rule: rat.check-equivalent, Check the result has the same values
      claim: equivalent expression
      explanation: Evaluate both at enough points to fix a rational function of this degree.
      detail: Two fractions of polynomials that agree at more points than the degree of their cross-multiplied difference are the same function wherever both are defined.
      proof obligation: obl.rational.same-values, the new form has the value of the old one wherever both are defined
      target claim: (x + (1 * ((x + -1)^-1)) + (1 * ((x + 1)^-1))) equals (((x^3) + x) * (((x^2) + (-1))^(-1))) wherever both are defined
      check method: exact evaluation at more points than the cross-multiplied degree
      expected: equal values
      observed: both sides agree at 7 points, more than the degree 6 their cross-multiplied difference can have, read from the polynomials
      verification: exact evaluation at more points than the cross-multiplied degree, passed, both sides agree at 7 points, more than the degree 6 their cross-multiplied difference can have, read from the polynomials
      verified: yes
