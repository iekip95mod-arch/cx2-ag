problem: 20.0 i + 0.0 j + 0.0 k cm cross 0.0 i + 15.0 j + 0.0 k N
variable: cross product
outcome: solved
result: 3 k kg m^2/s^2
context
  application version: nps 0.2
  capability manifest: <current>
  problem family: physics.vectors.cartesian-cross-product.three-dimension
  envelope version: <not recorded>
  normalized problem: vector_cross(framed_vector(vector(20, 0, 0), (1 * (100^-1)), lab, dimension(1, 0, 0, 0)), framed_vector(vector(0, 15, 0), 1, lab, dimension(1, 1, -2, 0)))
  requested method: convert components to SI, form the determinant cross product, report once
  active assumption: first vector frame is lab
  active assumption: second vector frame is lab
  angle convention: <not recorded>
  branch convention: <not recorded>
  unit policy: exact SI conversion and component arithmetic, final-only precision
  detail projection: standard
  resource policy: rewrites<=4096 steps<=512 branches<=64 backend<=32
  derivation status: solved and verified
steps
  1 plan
    phase: plan
    goal: Cross the two Cartesian vectors
    rule: vec.cross.plan, Cartesian vector cross product
    claim: no claim
    explanation: Check compatibility, convert to SI, then expand the determinant into three components
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Cartesian vector cross product
    applicability: both vectors have exactly rank three
    applicability: both vectors use the same frame
    applicability: the product of the two vectors' dimensions fits the dimension table
    fact: first vector: 20 i cm
    fact: second vector: 15 j N
    rationale: the determinant expansion after exact SI conversion gives an axis vector orthogonal to both operands
    verification: rank comparison, passed, ranks 3 and 3
    verification: frame identity, passed, frame lab and frame lab
    verification: dimensional multiplication, passed, L and L M T^-2
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    1.1 check
      phase: check
      goal: Check vector ranks
      rule: vec.cross.check-rank, Vector cross product precondition
      claim: definition
      explanation: The cross product is defined on two three-dimensional vectors, so both operands need all three axes
      proof obligation: obl.vector-cross.rank-three, both input vectors have exactly rank three
      target claim: both vectors are rank three
      check method: rank comparison
      expected: rank 3
      observed: ranks 3 and 3
      verification: rank comparison, passed, ranks 3 and 3
      verified: yes
    1.2 check
      phase: check
      goal: Check vector frames
      rule: vec.cross.check-frame, Vector cross product precondition
      claim: definition
      explanation: Frames must match unless a basis transformation is explicit
      proof obligation: obl.vector-cross.frames-match, both input vectors use the same frame
      target claim: both vectors use the same frame
      check method: frame identity
      expected: identical named frames
      observed: frame lab and frame lab
      verification: frame identity, passed, frame lab and frame lab
      verified: yes
    1.3 check
      phase: check
      goal: Check the product dimension
      rule: vec.cross.check-dimension, Vector cross product precondition
      claim: definition
      explanation: Unlike a sum, the two operand dimensions need not match: the product dimension is their product, such as a force crossed with a distance giving a torque
      proof obligation: obl.vector-cross.dimension-product, the product of the two operand dimensions fits the dimension table
      target claim: the product dimension fits
      check method: dimensional multiplication
      expected: a representable product dimension
      observed: L and L M T^-2
      verification: dimensional multiplication, passed, L and L M T^-2
      verified: yes
    1.4 transformation
      phase: solve
      goal: Convert both vectors to SI
      rule: vec.cross.convert-si, Exact SI conversion
      claim: equivalent expression
      explanation: Apply each unit's exact scale to every component
      detail: Reach for this whenever the two vectors are written in different units, or in any unit that is not the SI one. The determinant expansion below assumes both operands are already on the same scale, so every component is put in SI first. The scale factors are exact, so nothing is lost here and nothing needs rounding yet.
      proof obligation: obl.physics.converts-by-table, the SI form comes from the unit table's exact factor for the entered unit
      action: 20 i cm becomes 0.2 i m, and 15 j N becomes 15 j kg m/s^2
      before: vector_cross(framed_vector(vector(20, 0, 0), (1 * (100^-1)), lab, dimension(1, 0, 0, 0)), framed_vector(vector(0, 15, 0), 1, lab, dimension(1, 1, -2, 0)))
      after: vector_cross(framed_vector(vector((1 * (5^-1)), 0, 0), 1, lab, dimension(1, 0, 0, 0)), framed_vector(vector(0, 15, 0), 1, lab, dimension(1, 1, -2, 0)))
      reversible: yes
      verification: unit table, passed, both unit scales were applied exactly
      verified: yes
    1.5 transformation
      phase: solve
      goal: Expand the i determinant term
      rule: vec.cross.component-i, Determinant component expansion
      claim: equivalent expression
      explanation: Each axis is the difference of the two off-axis products
      detail: Reach for this once both vectors are in SI and you want their cross product. Every axis of a cross product is the difference of two products taken from the other two axes: the i component is ay*bz minus az*by, and the j and k components cycle the same pattern around the other axes. Each of those is ordinary exact multiplication and subtraction, which is the whole reason the determinant form is worth writing down.
      proof obligation: obl.vector-cross.component-cross, the component written down is the exact determinant expansion of the two input components
      action: ay*bz - az*by = 0
      before: ((0 * 0) + (-(0 * 15)))
      after: 0
      reversible: yes
      verification: exact rational arithmetic, passed, 0
      verified: yes
    1.6 transformation
      phase: solve
      goal: Expand the j determinant term
      rule: vec.cross.component-j, Determinant component expansion
      claim: equivalent expression
      explanation: Each axis is the difference of the two off-axis products
      detail: Reach for this once both vectors are in SI and you want their cross product. Every axis of a cross product is the difference of two products taken from the other two axes: the i component is ay*bz minus az*by, and the j and k components cycle the same pattern around the other axes. Each of those is ordinary exact multiplication and subtraction, which is the whole reason the determinant form is worth writing down.
      proof obligation: obl.vector-cross.component-cross, the component written down is the exact determinant expansion of the two input components
      action: az*bx - ax*bz = 0
      before: ((0 * 0) + (-((1 * (5^-1)) * 0)))
      after: 0
      reversible: yes
      verification: exact rational arithmetic, passed, 0
      verified: yes
    1.7 transformation
      phase: solve
      goal: Expand the k determinant term
      rule: vec.cross.component-k, Determinant component expansion
      claim: equivalent expression
      explanation: Each axis is the difference of the two off-axis products
      detail: Reach for this once both vectors are in SI and you want their cross product. Every axis of a cross product is the difference of two products taken from the other two axes: the i component is ay*bz minus az*by, and the j and k components cycle the same pattern around the other axes. Each of those is ordinary exact multiplication and subtraction, which is the whole reason the determinant form is worth writing down.
      proof obligation: obl.vector-cross.component-cross, the component written down is the exact determinant expansion of the two input components
      action: ax*by - ay*bx = 3
      before: (((1 * (5^-1)) * 15) + (-(0 * 0)))
      after: 3
      reversible: yes
      verification: exact rational arithmetic, passed, 3
      verified: yes
    1.8 check
      phase: check
      goal: Check orthogonality to the first operand
      rule: vec.cross.check-orthogonal-first, Vector cross product precondition
      claim: definition
      explanation: The cross product is defined to be perpendicular to both inputs, so its exact dot product with the first operand must be exactly zero
      proof obligation: obl.vector-cross.orthogonal-first, the result vector is orthogonal to the first operand
      target claim: the result is orthogonal to the first operand
      check method: exact dot product
      expected: exact dot product equal to zero
      observed: 0
      verification: exact dot product, passed, 0
      verified: yes
    1.9 check
      phase: check
      goal: Check orthogonality to the second operand
      rule: vec.cross.check-orthogonal-second, Vector cross product precondition
      claim: definition
      explanation: The same perpendicularity holds against the second operand
      proof obligation: obl.vector-cross.orthogonal-second, the result vector is orthogonal to the second operand
      target claim: the result is orthogonal to the second operand
      check method: exact dot product
      expected: exact dot product equal to zero
      observed: 0
      verification: exact dot product, passed, 0
      verified: yes
    1.10 check
      phase: check
      goal: Check anticommutativity
      rule: vec.cross.check-anticommutative, Vector cross product precondition
      claim: definition
      explanation: Swapping the operand order must negate every component exactly
      proof obligation: obl.vector-cross.anticommutative, crossing the operands in the opposite order negates the result exactly
      target claim: the reversed cross product negates this result
      check method: exact reversed cross product
      expected: exact componentwise negation
      observed: b cross a equals the negation of a cross b
      verification: exact reversed cross product, passed, b cross a equals the negation of a cross b
      verified: yes
    1.11 transformation
      phase: report
      goal: Apply final reporting precision
      rule: vec.cross.report-precision, Final-only precision
      claim: definition
      explanation: Round only the reported vector, after exact determinant expansion
      detail: Reach for this once, at the very end, and never partway through. A measured value is only as good as the figures it was written with, so the answer is reported to the fewest significant figures among the measurements it came from. Rounding a component before crossing would throw away figures the final rounding cannot get back, which is why every step above this one keeps the exact value.
      proof obligation: obl.vector-cross.rounding-final, precision is applied once after exact component cross
      proof obligation: obl.vector-cross.rounding-within-half-place, the reported vector is within half a unit in the last place of the exact one
      action: report 3 k kg m^2/s^2
      before: vector(0, 0, 3)
      after: vector(0, 0, 3)
      reversible: no
      verification: significant figures, passed, 1
      verification: exact half-place comparison, passed, 3 k kg m^2/s^2 is within half a unit in the last place of 3 k kg m^2/s^2
      verified: yes
