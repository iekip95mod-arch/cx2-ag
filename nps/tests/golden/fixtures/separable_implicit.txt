problem: desolve(y'=x/y^2,x,y)
outcome: solved
result: (((y^3) * (3^-1)) = (((x^2) * (2^-1)) + C))
context
  application version: nps 0.2
  capability manifest: <current>
  problem family: calculus.ode.separable.first-order
  envelope version: 1
  normalized problem: (diff(y, x) = (x * ((y^2)^(-1))))
  requested method: separate the variables, integrate both sides, then solve for the dependent variable where the relation allows
  active assumption: (y^-2) != 0
  angle convention: radians
  branch convention: real domain, principal values
  unit policy: exact integers, decimals as written, no unit conversion
  detail projection: standard
  resource policy: rewrites<=4096 steps<=512 branches<=64 backend<=32
  derivation status: solved and verified
steps
  1 plan
    phase: plan
    goal: Solve (diff(y, x) = (x * ((y^2)^(-1)))) for y
    rule: ode.separable.plan, Separation of variables
    claim: no claim
    explanation: Move every y to one side and every x to the other, integrate both sides, then solve for y if the relation allows
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Separate the variables
    applicability: the equation gives the first derivative of the dependent variable as an expression in the two variables
    applicability: the right side is a product of a factor in the independent variable alone and a factor in the dependent variable alone
    fact: the right side is x times (y^-2)
    rationale: a right side that splits into a factor of the independent variable and a factor of the dependent one can be separated and each side integrated on its own
    verification: structural reading of the equation, passed, one side is the first derivative of y
    verification: structural factor partition, passed, every factor mentions at most one of the variables
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
  2 transformation
    phase: separate
    goal: Separate the variables
    rule: ode.separable.separate, Separate the variables
    claim: implication
    explanation: Where (y^-2) != 0 the equation is the same as the separated one. A constant solution with (y^-2) = 0 is not reached
    detail: Reach for separation when the derivative is a product of a factor in the independent variable alone and one in the dependent variable alone. Dividing by the dependent factor is only valid where it is not zero, which is why a constant solution at one of its zeros is lost here.
    proof obligation: obl.ode.separation-divides-nonzero, dividing by the dependent factor keeps every solution on which it is not zero
    action: Divide both sides by (y^-2) and integrate each
    before: (diff(y, x) = (x * ((y^2)^(-1))))
    after: (int((y^2), y) = int(x, x))
    reversible: no
    verification: structural factor partition, passed, the factors split into x and (y^-2)
    verified: yes
  3 plan
    phase: plan
    goal: Integrate (y^2) with respect to y
    rule: calculus.integrate.rules, Integrate by rule
    claim: no claim
    explanation: Work outwards in, applying the rule that matches each form, then check by differentiating
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Integrate by rule
    applicability: every form in the integrand has an antiderivative rule
    applicability: every function argument and every power base is the variable or linear in it
    fact: integrate with respect to y
    rationale: each rule is applied to the form it matches and the parts are integrated in turn, and the answer is then differentiated to confirm it gives the integrand back
    verification: registered antiderivative rule dispatch, passed, every visited form matched a registered antiderivative rule
    verification: registered inner-form analysis, passed, every matched inner form met its registered linearity requirement
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    3.1 transformation
      phase: integrate
      goal: Integrate (y^2)
      rule: i.power, Power rule
      claim: equivalent expression
      explanation: Raise the exponent by one and divide by the new exponent
      detail: Reach for this when the variable is raised to a fixed whole number and that number is not minus one. It runs the power rule backwards: the exponent goes up by one and the whole thing is divided by that new exponent. Minus one is the one exponent this cannot do, because raising it by one gives zero and nothing may be divided by zero. That case is the natural logarithm instead.
      proof obligation: obl.calculus.rule-preserves-value, the rewritten subexpression has the value the original had
      action: Raise y to power 3 and divide by 3
      before: int((y^2), y)
      after: ((y^3) * (3^-1))
      reversible: no
      verification: rule-local invariant, passed, the base is the variable and the exponent is a constant integer other than minus one
      verified: yes
    3.2 check
      phase: check
      goal: Check the answer
      rule: calculus.integrate.check-by-differentiation, Check by differentiation
      claim: equivalent expression
      explanation: Differentiate the result and compare it with the integrand
      detail: An antiderivative is right exactly when its derivative is the integrand, so the check is to differentiate it by the same rules and compare the two forms.
      proof obligation: obl.integrate.derivative-returns-integrand, the derivative of the antiderivative is the integrand
      target claim: the derivative of ((y^3) * (3^-1)) is (y^2)
      check method: differentiate the result by rule and compare canonical forms
      expected: (y^2)
      observed: (y^2)
      verification: differentiate the antiderivative, passed, the derivative of the result is the integrand
      verified: yes
  4 plan
    phase: plan
    goal: Integrate x with respect to x
    rule: calculus.integrate.rules, Integrate by rule
    claim: no claim
    explanation: Work outwards in, applying the rule that matches each form, then check by differentiating
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Integrate by rule
    applicability: every form in the integrand has an antiderivative rule
    applicability: every function argument and every power base is the variable or linear in it
    fact: integrate with respect to x
    rationale: each rule is applied to the form it matches and the parts are integrated in turn, and the answer is then differentiated to confirm it gives the integrand back
    verification: registered antiderivative rule dispatch, passed, every visited form matched a registered antiderivative rule
    verification: registered inner-form analysis, passed, every matched inner form met its registered linearity requirement
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    4.1 transformation
      phase: integrate
      goal: Integrate x
      rule: i.power, Power rule
      claim: equivalent expression
      explanation: Raise the exponent by one and divide by the new exponent
      detail: Reach for this when the variable is raised to a fixed whole number and that number is not minus one. It runs the power rule backwards: the exponent goes up by one and the whole thing is divided by that new exponent. Minus one is the one exponent this cannot do, because raising it by one gives zero and nothing may be divided by zero. That case is the natural logarithm instead.
      proof obligation: obl.calculus.rule-preserves-value, the rewritten subexpression has the value the original had
      action: Raise x to power 2 and divide by 2
      before: int(x, x)
      after: ((x^2) * (2^-1))
      reversible: no
      verification: rule-local invariant, passed, the base is the variable and the exponent is a constant integer other than minus one
      verified: yes
    4.2 check
      phase: check
      goal: Check the answer
      rule: calculus.integrate.check-by-differentiation, Check by differentiation
      claim: equivalent expression
      explanation: Differentiate the result and compare it with the integrand
      detail: An antiderivative is right exactly when its derivative is the integrand, so the check is to differentiate it by the same rules and compare the two forms.
      proof obligation: obl.integrate.derivative-returns-integrand, the derivative of the antiderivative is the integrand
      target claim: the derivative of ((x^2) * (2^-1)) is x
      check method: differentiate the result by rule and compare canonical forms
      expected: x
      observed: x
      verification: differentiate the antiderivative, passed, the derivative of the result is the integrand
      verified: yes
  5 transformation
    phase: separate
    goal: Integrate both sides
    rule: ode.separable.integrate-both-sides, Integrate both sides
    claim: implication
    explanation: Two antiderivatives of equal differentials differ by a constant, so one C covers both sides
    detail: Once both sides are separated each integral is an ordinary antiderivative. Write one constant rather than one per side, because two free constants on opposite sides are the same freedom counted twice.
    proof obligation: obl.ode.integrals-differ-by-constant, two antiderivatives of equal differentials differ by one constant, and C names it
    action: Write both antiderivatives and one constant C
    before: (int((y^2), y) = int(x, x))
    after: (((y^3) * (3^-1)) = (((x^2) * (2^-1)) + C))
    reversible: no
    verification: rule-local invariant, passed, C appears nowhere in either antiderivative
    verified: yes
  6 check
    phase: check
    goal: Check the solution against the equation
    rule: ode.separable.check-solution, Check by differentiation
    claim: equivalent expression
    explanation: Differentiate each side of the relation and compare with the separated factors
    proof obligation: obl.ode.solution-satisfies-equation, the solution satisfies the differential equation
    target claim: the solution satisfies the differential equation
    check method: differentiate both sides of the relation
    expected: (y^2) and x
    observed: (y^2) and x
    verification: differentiate both sides of the relation, passed, the derivative matches the equation
    verified: yes
