problem: comb(8,3)
outcome: evaluated
result: 56
detail: All 3 consecutive product steps are checked. comb(8, 3) = 56
context
  application version: nps 0.2
  capability manifest: <current>
  problem family: number.integer-method.literal
  envelope version: 2
  normalized problem: comb(8, 3)
  requested method: comb
  angle convention: not applicable
  branch convention: nonnegative integers
  unit policy: exact integers, decimals as written, no unit conversion
  detail projection: standard
  resource policy: rewrites<=4096 steps<=512 branches<=64 backend<=32
  derivation status: solved and verified
steps
  1 plan
    phase: integer
    goal: comb(8, 3)
    rule: plan.integer-method, Build each binomial coefficient by an exact product and division
    claim: no claim
    explanation: Build each binomial coefficient by an exact product and division
    detail: comb(8, 3): Build each binomial coefficient by an exact product and division
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Build each binomial coefficient by an exact product and division
    applicability: nonnegative integer literals fit the method's stated bounds
    fact: comb(8, 3)
    rationale: The literal arguments fit this exact finite method
    verification: integer envelope validation, passed, comb(8, 3)
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    1.1 transformation
      phase: integer
      goal: comb(8, 3)
      rule: int.combination-product, Build each binomial coefficient by an exact product and division
      claim: equivalent expression
      explanation: Product 1 of 3: 1 * 6 = 6. C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i
      detail: Product 1 of 3: 1 * 6 = 6. C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i. Here 0 <= k <= n. Dividing the ordered choices by their i possible positions counts each unordered selection once. The recurrence C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i starts at 1. Choosing a subset or its complement has the same count
      proof obligation: obl.int.combination-product, Build each binomial coefficient by an exact product and division
      action: Product 1 of 3: 1 * 6 = 6. C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i
      before: (1 * 6)
      after: 6
      reversible: yes
      verification: binomial recurrence, passed, 1 * 6 = 1 * 6 + 0, with 0 <= remainder < divisor
      verified: yes
    1.2 transformation
      phase: integer
      goal: comb(8, 3)
      rule: int.combination-product, Build each binomial coefficient by an exact product and division
      claim: equivalent expression
      explanation: Product 2 of 3: 6 * 7 / 2 = 21. C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i
      detail: Product 2 of 3: 6 * 7 / 2 = 21. C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i. Here 0 <= k <= n. Dividing the ordered choices by their i possible positions counts each unordered selection once. The recurrence C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i starts at 1. Choosing a subset or its complement has the same count
      proof obligation: obl.int.combination-product, Build each binomial coefficient by an exact product and division
      action: Product 2 of 3: 6 * 7 / 2 = 21. C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i
      before: ((6 * 7) * (2^(-1)))
      after: 21
      reversible: yes
      verification: binomial recurrence, passed, 6 * 7 = 2 * 21 + 0, with 0 <= remainder < divisor
      verified: yes
    1.3 transformation
      phase: integer
      goal: comb(8, 3)
      rule: int.combination-product, Build each binomial coefficient by an exact product and division
      claim: equivalent expression
      explanation: Product 3 of 3: 21 * 8 / 3 = 56. C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i
      detail: Product 3 of 3: 21 * 8 / 3 = 56. C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i. Here 0 <= k <= n. Dividing the ordered choices by their i possible positions counts each unordered selection once. The recurrence C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i starts at 1. Choosing a subset or its complement has the same count
      proof obligation: obl.int.combination-product, Build each binomial coefficient by an exact product and division
      action: Product 3 of 3: 21 * 8 / 3 = 56. C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i
      before: ((21 * 8) * (3^(-1)))
      after: 56
      reversible: yes
      verification: binomial recurrence, passed, 21 * 8 = 3 * 56 + 0, with 0 <= remainder < divisor
      verified: yes
    1.4 transformation
      phase: integer
      goal: comb(8, 3)
      rule: int.combination-product, Build each binomial coefficient by an exact product and division
      claim: equivalent expression
      explanation: All 3 consecutive product steps are checked. comb(8, 3) = 56
      detail: All 3 consecutive product steps are checked. comb(8, 3) = 56. Here 0 <= k <= n. Dividing the ordered choices by their i possible positions counts each unordered selection once. The recurrence C(n-k+i,i) = C(n-k+i-1,i-1) * (n-k+i) / i starts at 1. Choosing a subset or its complement has the same count
      proof obligation: obl.int.combination-product, Build each binomial coefficient by an exact product and division
      action: All 3 consecutive product steps are checked. comb(8, 3) = 56
      before: comb(8, 3)
      after: 56
      reversible: yes
      verification: binomial recurrence, passed, All 3 consecutive product steps are checked. comb(8, 3) = 56
      verified: yes
