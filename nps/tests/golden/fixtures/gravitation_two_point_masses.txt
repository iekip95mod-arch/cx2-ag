problem: two point masses of 2 kg and 3 kg, 1 m apart; find the gravitational force
variable: gravitational force
outcome: solved
result: 0.00000000040044 kg m/s^2
context
  application version: nps 0.2
  capability manifest: <current>
  problem family: physics.gravitation.point-masses
  envelope version: <not recorded>
  normalized problem: (F = (((G * m1) * m2) * (r^-2)))
  requested method: Newtonian gravitation, exact SI substitution, linear isolation of the requested quantity
  active assumption: each body is a point mass, or a sphere whose mass distribution is spherically symmetric so its field outside equals that of a point mass at its centre
  active assumption: the separation is measured between the two centres and no third body contributes
  active assumption: the tabulated gravitational constant 6.674e-11 m^3 kg^-1 s^-2 is used exactly and its measurement uncertainty is not propagated
  angle convention: <not recorded>
  branch convention: real domain
  unit policy: validate dimensions before substitution, convert exactly to SI, and round only the reported answer
  detail projection: standard
  resource policy: rewrites<=4096 steps<=512 branches<=64 backend<=32
  derivation status: solved and verified
steps
  1 plan
    phase: plan
    goal: Find gravitational force
    rule: physics.gravitation.definition, Newton's law of gravitation
    claim: no claim
    explanation: Use F = G*m1*m2*r^-2 and solve for the requested quantity
    assumption before: each body is a point mass, or a sphere whose mass distribution is spherically symmetric so its field outside equals that of a point mass at its centre
    assumption before: the separation is measured between the two centres and no third body contributes
    assumption before: the tabulated gravitational constant 6.674e-11 m^3 kg^-1 s^-2 is used exactly and its measurement uncertainty is not propagated
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Apply Newton's law of gravitation F = G*m1*m2/r^2
    applicability: every quantity in F = G*m1*m2*r^-2 uses compatible dimensions
    applicability: the relation is linear in gravitational force
    fact: first mass = 2 kg
    fact: second mass = 3 kg
    fact: separation = 1 m
    fact: find gravitational force
    alternative: general symbolic backend isolation
    rationale: F = G*m1*m2*r^-2 is linear in the requested unknown after exact SI substitution, so the local exact linear solver is sufficient and deterministic
    verification: dimensional analysis, passed, L M T^-2 against L M T^-2
    verification: registered relation-position model, passed, F occurs at power one in F = G*m1*m2*r^-2
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    1.1 check
      phase: check
      goal: Check the dimensions of F = G*m1*m2*r^-2
      rule: physics.gravitation.check-dimensions, Dimensional analysis
      claim: definition
      explanation: Both sides of F = G*m1*m2*r^-2 must have the same dimension
      proof obligation: physics.gravitation.dimensions-agree, both sides of F = G*m1*m2*r^-2 have the same dimension
      target claim: F = G*m1*m2*r^-2 is dimensionally consistent
      check method: raise each factor dimension to its power and multiply
      expected: equal dimensions
      observed: L M T^-2 against L M T^-2
      verification: dimensional analysis, passed, L M T^-2 against L M T^-2
      verified: yes
    1.2 transformation
      phase: solve
      goal: Substitute the known SI values
      rule: physics.gravitation.substitute, Substitution
      claim: solution set preserved
      explanation: Replace each known symbol by its exact SI value
      detail: After converting the known quantities to SI, put their values into F = G*m1*m2/r^2. Leave the requested quantity as a symbol, then solve the resulting equation for it.
      proof obligation: obl.physics.lookup-preserves-solutions, the value put in place of a symbol is the one the problem declared for it
      action: Substitute the known quantities into F = G*m1*m2*r^-2 after exact SI conversion
      before: (F = (((G * m1) * m2) * (r^-2)))
      after: (F = ((((6674 * (100000000000000^-1)) * 2) * 3) * (1^-2)))
      reversible: yes
      verification: typed known-quantity lookup, passed, 3 known quantities were substituted
      verified: yes
    1.3 check
      phase: check
      goal: Check the candidate in F = G*m1*m2*r^-2
      rule: physics.gravitation.check-candidate, Substitution check
      claim: implication
      explanation: Put the candidate back into F = G*m1*m2*r^-2
      proof obligation: physics.gravitation.candidate-satisfies, the candidate satisfies F = G*m1*m2*r^-2
      target claim: F = 0.00000000040044 satisfies F = G*m1*m2*r^-2
      check method: substitute every exact SI value into the relation
      expected: both sides of F = G*m1*m2*r^-2 agree
      observed: both sides are exactly equal
      verification: exact substitution into the original relation, passed, both sides are exactly equal
      verified: yes
  2 plan
    phase: plan
    goal: Isolate F
    rule: eq.linear.inverse-operations, Inverse operations on a linear equation
    claim: no claim
    explanation: Solve by undoing what was done to F
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Inverse operations on a linear equation
    applicability: the equation is degree one in F
    applicability: every other quantity is a rational constant
    fact: one unknown, F
    rationale: collect the terms, remove the constant and isolate F using its non-zero coefficient, or inspect the remaining constant if the coefficient is zero
    verification: exact linear analysis, passed, both sides reduced to a coefficient and constant term
    verification: exact linear analysis, passed, all coefficients and constants are exact rationals
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    2.1 transformation
      phase: solve
      goal: Collect the terms in F
      rule: eq.collect-like-terms, Collect like terms
      claim: solution set preserved
      explanation: Moving every term to one side keeps both sides equal
      detail: Reach for this whenever the unknown appears more than once, on either side or both. Adding or subtracting the same quantity from both sides leaves the equation with the same solutions, so the terms in the unknown can all be gathered on one side and the plain numbers on the other without changing the answer.
      proof obligation: obl.eq.same-solutions, the rewritten equation has the solutions the original had
      action: Move every term to the left side
      before: (F = ((((6674 * (100000000000000^-1)) * 2) * 3) * (1^-2)))
      after: (((1 * F) + (-10011 * (25000000000000^(-1)))) = 0)
      reversible: yes
      verification: rule-local equality invariant, passed, the same quantity was added to both sides, which cannot change the solution set
      verified: yes
    2.2 transformation
      phase: solve
      goal: Isolate F
      rule: eq.divide-both-sides, Isolate the unknown
      claim: solution set preserved
      explanation: Applying the same operation to both sides preserves the solutions
      detail: Remove the constant term by applying its opposite to both sides. The coefficient of F is 1, so no division is needed.
      proof obligation: obl.eq.same-solutions, the rewritten equation has the solutions the original had
      action: Add 0.00000000040044 to both sides
      before: (((1 * F) + (-10011 * (25000000000000^(-1)))) = 0)
      after: (F = (10011 * (25000000000000^(-1))))
      reversible: yes
      verification: rule-local equality invariant, passed, removing the constant preserves equality, and the coefficient 1 was checked to be non-zero
      verified: yes
    2.3 check
      phase: check
      goal: Check the answer
      rule: eq.linear.check-by-substitution, Check by substitution
      claim: solution set preserved
      explanation: Put the answer back into the original equation
      detail: Put the candidate back where the unknown was in the equation as it was typed and work both sides out. A solution makes them equal, so anything else means the candidate is not one and the answer is withheld rather than reported.
      proof obligation: obl.linear.candidate-satisfies, the candidate satisfies the original equation
      target claim: F = (10011 * (25000000000000^(-1))) satisfies the equation
      check method: substitute the solution into the equation as it was typed
      expected: both sides equal
      observed: both sides are equal
      verification: substitution, passed, both sides came out equal
      verified: yes
