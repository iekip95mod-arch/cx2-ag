problem: two-slit interference; find wavelength; slit spacing = 1 mm; fringe sine = 0.0012; fringe order = 2
variable: wavelength
outcome: solved
result: 0.00000060 m
context
  application version: nps 0.2
  capability manifest: <current>
  problem family: physics.optics.double-slit.maxima
  envelope version: <not recorded>
  normalized problem: ((d * sin(t)) = (m * lambda))
  requested method: Two-slit interference maxima, exact SI substitution, exact rational isolation
  active assumption: the medium is homogeneous and the light is monochromatic
  active assumption: the order m counts bright fringes outward from the central maximum, so m is 0, 1, 2 and up
  angle convention: <not recorded>
  branch convention: real domain
  unit policy: validate dimensions before substitution, convert exactly to SI, and round only the reported answer
  detail projection: standard
  resource policy: rewrites<=4096 steps<=512 branches<=64 backend<=32
  derivation status: solved and verified
steps
  1 plan
    phase: plan
    goal: Find wavelength
    rule: physics.optics.double-slit.maxima, Two-slit interference maxima
    claim: no claim
    explanation: Use d*sin(t) = m*lambda and solve for the requested quantity
    proof obligation: obl.plan.preconditions-hold, every registered strategy precondition has passing evidence
    strategy: Apply Two-slit interference maxima as d*sin(t) = m*lambda
    applicability: both sides of the relation have the same dimension
    applicability: the sign and order convention is declared before it is used
    applicability: every given quantity is inside this family's stated domain
    fact: slit spacing = 1 mm
    fact: fringe sine = 0.0012
    fact: fringe order = 2
    fact: find wavelength
    alternative: general symbolic backend isolation
    rationale: every unknown of this relation is reached by exact rational arithmetic on the remaining quantities, so the local solver is sufficient and deterministic
    verification: dimensional analysis, passed, L against L
    verification: registered relation convention, passed, the order m counts bright fringes outward from the central maximum, so m is 0, 1, 2 and up
    verification: registered optical domain rules, passed, every given quantity is inside this family's stated domain
    verification: registered strategy preconditions, passed, every registered strategy precondition has passed evidence
    verified: nothing claimed
    1.1 check
      phase: check
      goal: Check the dimensions of d*sin(t) = m*lambda
      rule: physics.optics.check-dimensions, Dimensional analysis
      claim: definition
      explanation: Both sides of the optical relation must have the same dimension
      proof obligation: obl.optics.dimensions-agree, both sides of the optical relation have one dimension
      target claim: d*sin(t) = m*lambda is dimensionally consistent
      check method: combine the dimensions on each side
      expected: equal dimensions
      observed: L against L
      verification: dimensional analysis, passed, L against L
      verified: yes
    1.2 transformation
      phase: solve
      goal: Convert the known quantities to SI
      rule: physics.optics.convert-units, Unit conversion
      claim: solution set preserved
      explanation: Apply each unit's exact scale to SI
      detail: Use metres for every length in d*sin(t) = m*lambda so the two sides use consistent units. A wavelength in nanometres and a slit spacing in millimetres cancel only after both reach metres.
      proof obligation: obl.physics.scale-preserves-solutions, each quantity's stored SI value is its given value times the table factor
      action: slit spacing = 1 mm becomes slit spacing = 0.001 m
      before: ((d * sin(t)) = (m * lambda))
      after: ((d * sin(t)) = (m * lambda))
      reversible: yes
      verification: divide each stored value by its table scale and compare with the given, passed, 3 stored values divide back to the given by their table scale
      verified: yes
    1.3 check
      phase: check
      goal: Check the givens against the optical domain
      rule: physics.optics.check-domain, Optical domain rules
      claim: definition
      explanation: An index is at least one, a sine lies in [0, 1], a length is positive and an order is a whole number
      proof obligation: obl.optics.domain-holds, every quantity the relation uses lies inside this family's stated domain
      target claim: the givens are physically admissible for Two-slit interference maxima
      check method: apply each registered domain rule to the converted SI value
      expected: every rule holds
      observed: every given quantity is inside this family's stated domain
      verification: registered optical domain rules, passed, every given quantity is inside this family's stated domain
      verified: yes
    1.4 check
      phase: solve
      goal: State the convention the answer is read under
      rule: physics.optics.sign-convention, Declared convention
      claim: definition
      explanation: the order m counts bright fringes outward from the central maximum, so m is 0, 1, 2 and up
      detail: Nothing below this line decides a sign on its own. the order m counts bright fringes outward from the central maximum, so m is 0, 1, 2 and up. Read every answer of this family against that sentence, because the same arithmetic under the opposite convention names a different physical situation.
      assumption before: the order m counts bright fringes outward from the central maximum, so m is 0, 1, 2 and up
      proof obligation: obl.optics.convention-declared, the sign and order convention is stated before any signed answer is read
      target claim: the convention is visible rather than baked into the arithmetic
      check method: read the convention registered with the relation
      expected: a nonempty declared convention
      observed: the order m counts bright fringes outward from the central maximum, so m is 0, 1, 2 and up
      verification: registered relation convention, passed, the order m counts bright fringes outward from the central maximum, so m is 0, 1, 2 and up
      verified: yes
    1.5 transformation
      phase: solve
      goal: Substitute the known SI values
      rule: physics.optics.substitute, Substitution
      claim: solution set preserved
      explanation: Replace each known symbol by its exact SI value
      detail: Put the converted quantities into d*sin(t) = m*lambda, leave the requested quantity as a symbol, and isolate it with exact rational arithmetic.
      proof obligation: obl.physics.lookup-preserves-solutions, the value put in place of a symbol is the one the problem declared for it
      action: Substitute the known quantities after exact SI conversion
      before: ((d * sin(t)) = (m * lambda))
      after: (((1 * (1000^-1)) * (3 * (2500^-1))) = (2 * lambda))
      reversible: yes
      verification: typed known-quantity lookup, passed, 3 known quantities were substituted
      verified: yes
    1.6 check
      phase: check
      goal: Check the candidate in d*sin(t) = m*lambda
      rule: physics.optics.check-candidate, Substitution check
      claim: implication
      explanation: Put the candidate back into the optical relation
      proof obligation: obl.optics.candidate-satisfies, the candidate satisfies the original optical relation
      target claim: lambda = 0.0000006 satisfies d*sin(t) = m*lambda
      check method: evaluate both sides from the exact SI values
      expected: the two sides are equal
      observed: both sides are exactly equal
      verification: exact substitution into the original relation, passed, both sides are exactly equal
      verified: yes
  2 transformation
    phase: report
    goal: Report the answer to the measured precision
    rule: physics.optics.significant-figures, Significant figures
    claim: no claim
    explanation: Use the fewest significant figures among the measured givens
    detail: A wavelength written to three figures cannot buy a fringe spacing written to six, so the answer is reported to the fewest significant figures among the measurements it came from. Every step above this one keeps the exact value, because rounding partway through throws away figures the final rounding cannot get back.
    proof obligation: obl.physics.reported-within-half-place, the reported value is within half a unit in the last place of the exact one
    action: Report 0.0000006 as 0.00000060
    before: (3 * (5000000^-1))
    after: 0.00000060
    reversible: no
    verification: exact comparison against the unrounded value, passed, 0.00000060 is within half a unit in the last place of 0.0000006
    verified: nothing claimed
