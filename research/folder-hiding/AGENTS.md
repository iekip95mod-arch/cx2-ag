# Working on test_app

Read README.md before changing or deploying this prototype. It contains the evidence and completion record. Do not interpret a successful build as a successful boot or restoration test.

This prototype targets casual concealment of personal calculator documents. Its visible exception is test_app.tns, a working calculator. Entering exactly 1+9+9+8, allowing spaces, restores the hidden documents and displays 27. Preserve that behavior and keep restore prompts out of the app. The original PIN probes are historical experiments, not the current interface. The prototype's visibility target does not override a later user instruction for the active StepCAS handheld layout.

Personal documents belong in /appdata/test_app/store on the calculator. Relocated ndl belongs in /appdata/ndl. Both are outside the OS document browser root. Preserve only the exact test_app.tns document at the root plus factory files that match the manifest. The native calculator bridge belongs under external ndl. Staging it permanently in documents would leave an extra visible file.

Factory recognition is per file. The factory-6.4 manifest comes from the OS package and includes contents as well as names. Do not preserve an entire folder just because TI created it. The physical read retained for this experiment is OS 6.40.74, Boot1 5.0.42 and Boot2 6.20.7 on hardware 0x1d. OS 6.20.333 belongs to the original keysvc report. Recheck device identity and inventory the current documents before deploying a broad hide. Compare name, size and CRC32 even when the OS version matches.

The separate relocation patch selects /appdata/ndl when its resources file passes stat and otherwise tries /documents/ndl. The ordinary SDK runtime uses /documents/ndl. Successful stat does not validate contents. Patched startup, configuration and persistence use the selected root. Patched Lua module lookup searches external ndl before ordinary documents. Keep these decisions consistent when changing the patch. External boot passed with the earlier runtime spelling in the emulator. The renamed build and documents-only fallback still require runtime verification.

Manifest records must retain every original location. Write and close the manifest before moving contents. Preflight every unfinished batch before restoring. Identical regular files may remain at both locations and the stored copy must be retained when completing that batch. Differing files and directory collisions must refuse restoration and preserve both entries. Never delete metadata or create a done marker to suppress an error. An unfinished batch must be restored before another hide. Keep document transfers and edits idle during storage operations.

Use the private emulator file-hide-research for this work. Its flash belongs to this experiment. Other emulator sessions and shared ndl sources may belong to other work. Build the relocation patch in an isolated source copy. The pre-relocation flash backup this file used to name lived at /tmp/cx2-hide-research/pre-relocation.img and is gone, so there is no retained pre-relocation flash for this experiment. Take a fresh one before any relocation work and keep it in the workspaces directory, never under /tmp.

Keep source, tests, factory provenance and test evidence together here. Record exact runtime artifacts and fresh test outcomes in README.md. The retained runtime manifest is the artifact identity record. Its build-lane validation statement does not supersede later live evidence. Preserve failures that explain a limitation, including the debugger-injected UI failures. Launch UI probes from the normal document browser.

The completion record preserves host-suite results and an independent review from before the later identical-file collision fix. The earlier emulator runtime booted externally, answered service query 6b 01 02 07 and retained only Examples, MyLib, MyWidgets, PyLib and test_app after HideAll and another reboot. Native module loading and its restore callback worked. Restore stopped because the OS recreated the empty NspireLogs.zip. The identical-file fix has host evidence but its updated emulator module remains pending. These are historical results for the recorded artifacts. No browser-refresh defect was established by that failure.

Live secret restoration and physical migration remain pending until their evidence is appended to README.md. Do not turn these pending checks into pass claims from source inspection or host tests. Keep bridge_probe.lua as a diagnostic document. Its explicit restore wording must not enter test_app.

Revalidate boot persistence, factory preservation, the secret calculation, ordinary calculations and byte-identical restoration after changing the storage format or ndl location. Preserve the old runtime and current-document boot files during migration. Inspect rollback logs before retrying a partial migration. Never run another installer over a still-resident runtime. Recovery must remain possible from the saved originals.

The device transfer tool and the native filesystem use different namespaces. The retained original link observation accepted /ndless/startup/keysvc.tns where /documents/ndless/startup/keysvc.tns failed. Current source uses ndl. Stage through the transfer namespace verified for the selected build. Use native absolute filesystem paths for external storage and migration. A keysvc accepted response proves packet acceptance only. Inspect the resulting screen and verify reboot completion separately.
